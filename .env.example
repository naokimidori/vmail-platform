# API base URL for all requests. Use "mock" for local demo data.
API_BASE_URL=https://api.example.com

# Mailbox domains (comma-separated, first is default)
MAILBOX_DOMAINS=example.com,mail.example.net

# Public mailbox URL for access links
PUBLIC_MAILBOX_URL=https://mail.example.com
